Lo primero será instalar node.js  instálalo desde nodejs.org. Luego, crea un nuevo proyecto Node.js e instala Nodemailer:

mkdir miProyectoCorreo
cd miProyectoCorreo
npm init -y
npm install nodemailer
